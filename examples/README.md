# Examples

- `examples/basic`: minimal registry for quick validation and schema shape reference.
- `examples/demo`: full end-to-end exemplar (validation, lock check/update, `generate proto`, Buf lint/build/generate, generated Go/Python output locations).

Start with `examples/basic` to learn the YAML surface, then use `examples/demo` for the complete production workflow.
