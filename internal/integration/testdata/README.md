# Integration testdata

This directory is reserved for integration-test fixtures when needed.

The demo proto integration test copies `examples/demo` into a temporary directory at runtime and does not mutate repository fixtures in place.
